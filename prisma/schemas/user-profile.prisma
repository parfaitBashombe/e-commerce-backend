model UserProfile {
    user_profile_id String @id @default(uuid())
    user_id         String
    User            User   @relation(fields: [user_id], references: [user_id])

    @@map("user_profiles")
}
