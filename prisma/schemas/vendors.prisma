model Vendor {
    vendor_id String        @id @default(uuid())
    email     String        @unique
    fullname  String?
    password  String
    salt      String
    Product   Product[]
    OrderItem OrderItem[]
    status    VendorStatus  @default(PENDING)
    createdAt DateTime      @default(now())

    @@map("vendors")
}
